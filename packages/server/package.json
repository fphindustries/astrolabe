{
  "name": "@astrolabe/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "HTTP API, event log, projections, AI provider abstraction, prompt assembly",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "dev": "tsx watch src/http/serve.ts",
    "start": "node dist/http/serve.js",
    "migrate": "tsx src/db/cli.ts",
    "harness": "tsx src/harness/cli.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.125.0",
    "@astrolabe/rules": "*",
    "@astrolabe/shared": "*",
    "fastify": "^5.6.0",
    "postgres": "^3.4.0",
    "zod": "^4.1.0"
  }
}
