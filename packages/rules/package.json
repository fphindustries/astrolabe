{
  "name": "@astrolabe/rules",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Rules schema, Datasworn adapter, move automation, and dice. Pure functions, no I/O.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  },
  "comment": "No runtime dependencies by design (design record section 9). Datasworn is consumed at build time by the adapter (D-63).",
  "devDependencies": {
    "@datasworn/core": "0.0.10",
    "@datasworn/starforged": "0.0.10"
  }
}
