/**
 * Rules text for display. Imported prose keeps Datasworn's markdown links,
 * rewritten to Astrolabe IDs (`[Face Danger](id:move:adventure/face-danger)`),
 * so automation can follow them. A reader should see only the label.
 */
export * from './plain.js';
