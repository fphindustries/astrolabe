/**
 * Characters: what one is at creation, and what makes a draft valid
 * (task 3.1). Pure, and shared — the client validates a field as it is
 * edited, the server validates again before it writes.
 */
export * from './creation.js';
