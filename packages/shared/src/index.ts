/**
 * Event and DTO types shared by the server and the client.
 *
 * Event payload schemas land here in task 2.2. Until then this package exists
 * so the workspace graph and the project references are real.
 */

/** Placeholder until task 2.2 defines the event types. */
export const SHARED_PACKAGE = '@astrolabe/shared' as const;
