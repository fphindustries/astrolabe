# Astrolabe — Design Record

A living document. Each design round adds its approved decisions here, so nothing lives only in chat history. Status markers: **Approved**, **Draft**, **Open**.

*This project was called Forge Link during design rounds 1–5.*

---

## 1. Product Charter — v1.0 (Approved)

Astrolabe is an AI Game Guide for Ironsworn: Starforged. Players make the decisions, the AI turns them into vivid narrative and runs the world, and the app tracks everything so nobody is ever unsure what's happening or what to do next.

The AI Guide has three jobs: **narrate** (give player decisions narrative depth), **nudge** (help when the table stalls), and **track** (keep all state visible and current).

**First user:** Christopher, playing solo. **Target:** a private deployment for a few friends' tables, up to six players online together for a scheduled evening, with widely varying rules fluency.

**Guiding principles:** Players decide, and the AI adds depth. The app always has an answer to "what now?" Every development milestone ends in something playable. Solo comes first, but no decision may turn six-player play into a rewrite.

| Scope | |
|---|---|
| **In** | Single user controlling one or more characters, AI Guide, official Starforged rules, all state tracked digitally |
| **Designed for, not built yet** | Multiple human players, real-time sync (the data model records actor and character ownership from day one) |
| **Out** | Homebrew, public product, asynchronous play |

---

## 2. Decision Log

| ID | Decision | Round | Status |
|---|---|---|---|
| D-01 | Clean-slate build; nothing carried over from prior attempts. | 1 | Approved |
| D-02 | First user is Christopher solo; end goal is a private deployment for friends' tables. Public product is out of scope. | 1 | Approved |
| D-03 | Multiplayer means synchronous, scheduled sessions. Async play is out of scope. | 1 | Approved |
| D-04 | Up to six players. The UI must serve rules veterans and story-focused newcomers alike. | 1 | Approved |
| D-05 | Official Starforged rules only; no homebrew. | 1 | Approved |
| D-06 | For solo playtesting, one user may control multiple characters. | 2 | Approved |
| D-07 | Narration latitude setting (Minimal / Color / Full voice), with a campaign default and per-player override. | 2 | Approved |
| D-08 | On a miss, Pay the Price is offered with its rule options, with the oracle table roll as the highlighted default. The AI narrates the result but does not choose it. | 2 | Approved |
| D-09 | The AI grounds generated story content in oracle rolls (see §4). | 2 | Approved (details closed by D-65) |
| D-10 | Nudges happen only on request ("What now?") and take the form of suggested actions. | 2 | Approved |
| D-11 | Narration length scales to the dramatic weight of the moment, with a campaign-level adjustment. | 2 | Approved |
| D-12 | Sessions run 3–4 hours and open with an AI recap. | 2 | Approved |
| D-13 | The playtest uses a fresh campaign. | 2 | Approved |
| D-14 | Players can pick their move directly from a relevant-moves panel that is always available. AI move suggestions are optional help, not a required step. | 3 | Approved |
| D-15 | When a weak-hit complication has no menu, the player writes it or asks the AI for oracle-inspired options and picks one. | 3 | Approved |
| D-16 | For suffer moves, the AI proposes an amount based on the fiction and the player adjusts it before it applies. | 3 | Approved |
| D-17 | AI oracle rolls appear as chips under the narration they inspired. | 3 | Approved |
| D-18 | When an oracle result doesn't fit the fiction, the AI rerolls it visibly. The discarded result stays visible, struck through. | 3 | Approved (struck-through display is Draft) |
| D-19 | AI narration commits immediately; players correct it afterward rather than approving each passage. | 3 | Approved |
| D-20 | Roll results lead with the outcome; the full dice math opens in a popup. | 3 | Approved |
| D-21 | Astrolabe owns its internal rules schema. Datasworn's Starforged data is imported through an adapter and can be replaced if it ever constrains the app. | 4 | Approved |
| D-22 | Moves use three automation levels (Automated, Guided, Reference), assigned per move category (§5). | 4 | Approved |
| D-23 | Combat moves are Guided and ship in Milestone 2. | 4 | Approved (confirmed by D-76) |
| D-24 | Asset abilities are Guided. Only assets used by the playtest characters get full automation. | 4 | Approved |
| D-25 | The relevant-moves panel is rules-based, driven by explicit situation state. | 4 | Approved |
| D-26 | Players can manually override any meter, track, or clock; every override is logged. | 4 | Approved |
| D-27 | Players can void a roll and redo it; the voided roll stays in the log. | 4 | Approved |
| D-28 | The AI may set the odds on its own yes/no oracle questions about the world. | 4 | Approved |
| D-29 | Oracle rerolls are capped per result. The cap is campaign-configurable, default 2. | 4 | Approved |
| D-30 | Target: about 30 minutes to create a character. Characters are created before the session, not during it. | 5 | Approved (confirmed by D-77) |
| D-31 | Truths offer all three options per question: pick, roll, or write your own. No one-line-pitch generation. | 5 | Approved |
| D-32 | The starting sector is a list of locations and routes. A visual starmap comes in a later phase. | 5 | Approved |
| D-33 | Character creation is concept-first: the player describes a character, and the AI proposes the mechanical build for review. Every field stays directly editable. | 5 | Approved |
| D-34 | The AI proposes several inciting incidents, grounded in oracles and character backgrounds; the player may instead write their own. | 5 | Approved |
| D-35 | Lines and veils are deferred past Milestone 1. | 5 | Approved |
| D-36 | AI-generated portraits for characters, ships, and NPCs, for visual consistency. Deferred if they complicate the build. | 5 | Approved (later phase) |
| D-37 | If a chosen move's trigger doesn't match the described action, the AI notes it without blocking the roll. | 5 | Approved |
| D-38 | The project is named **Astrolabe** (previously Forge Link, renamed to avoid confusion with Crew Link). Tagline: *know where you are.* | — | Approved |
| D-39 | Design documentation lives in the project repository under `docs/`, versioned alongside the code. | — | Approved |
| D-40 | Three zones are always visible without scrolling: the scene header, the action composer, and the pressure rail (clocks, vows, progress). Only the narrative log scrolls. | 6 | Approved |
| D-41 | The centre column holds both a compact scene header and a flowing narrative log. | 6 | Approved |
| D-42 | Crew mini-cards show callsign, health, and momentum only. Everything else opens in a drawer. | 6 | Approved |
| D-43 | Scenes are explicit but low-ceremony: the AI proposes a scene change inline and the player accepts or ignores it. | 6 | Approved |
| D-44 | Desktop and laptop are the target. Tablet and mobile are out of scope for now. | 6 | Approved |
| D-45 | Dice rolls are animated. | 6 | Approved |
| D-46 | Visual direction follows Starforged's: dark surfaces, amber accents, similar typographic rhythm. No book art or layout is reproduced. | 6 | Approved |
| D-47 | React and TypeScript on the front end. | 7 | Approved |
| D-48 | PostgreSQL for storage. | 7 | Approved |
| D-49 | TypeScript on the server too, in a monorepo, so the rules engine and its types are shared with the client. | 7 | Approved |
| D-50 | Pluggable AI provider abstraction from the start, targeting Claude and OpenAI. | 7 | Approved |
| D-51 | A visible token counter per session. No hard budget or cap. | 7 | Approved |
| D-52 | No auth in Milestone 1 (single local user). Authentik via OIDC arrives with multiplayer. | 7 | Approved |
| D-53 | If the AI provider is unavailable, the session stops. Campaign state must survive intact. | 7 | Approved |
| D-54 | Narration starts streaming within 5 seconds. | 7 | Approved |
| D-55 | Docker packaging for production on a Linux home server. Local development runs natively. | 7 | Approved |
| D-56 | Milestone 1 is done when the golden session runs end to end with real rules data and real AI narration. Nothing beyond it ships. | 8 | Approved |
| D-57 | Build order runs rules engine → event log → creation flows → play shell → move flow → AI → polish (see `milestone-1.md`). **Amended, round 18:** the provider core (7.1, 7.2, 7.4, 7.5) comes before every task that needs a working AI provider: concept-first creation (3.3), 4.4's AI-proposal half, 7.12 and 7.13 (D-120, D-121), and groups 8 and 9. The reason: those tasks can't be built or tested without it, so an order that listed 3.3 as an ordinary creation-flow task hid a dependency. Group 7 is not a block that must finish first. In practice 7.1–7.11 landed together, so the order is 7.1–7.11 · 3.3 · 4.6 (D-126) · 7.12, 7.13 · 8 · 9 · 10. | 8 | Approved (groups 3–5 resequenced by D-88; AI-dependent tasks sequenced after the provider core, round 18) |
| D-58 | The build prompt targets Claude Code. Repository conventions live in `CLAUDE.md`. | 8 | Approved |
| D-59 | Milestone 1 hand-writes move automation only for the moves the golden session exercises (about eleven, including Swear an Iron Vow for campaign setup and Reach a Milestone for Beat 10). The rest of the Session, Adventure, Quest, Fate and Suffer categories, and all three Threshold moves, stay at Reference. This narrows §5's assignment table for Milestone 1 only. | 9 | Approved |
| D-60 | Milestone 1 ships the AI provider interface, the Claude implementation, and the stubbed provider the golden-session test needs. The OpenAI implementation moves to Milestone 2. D-50 is satisfied by the abstraction existing. | 9 | Approved |
| D-61 | When a golden-session beat contradicts the rules data, the rules data wins; the beat is amended. | 9 | Approved |
| D-62 | Aid Your Ally is a flag on a single move invocation, not a separately logged move. On a hit, effect targets resolve to the aided character. | 9 | Approved |
| D-63 | The Datasworn adapter targets version 0.0.10, consumed at build time as a development dependency. | 9 | Approved |
| D-64 | §7, §9, §10 and §12 are promoted from Draft to Approved. | 9 | Approved |
| D-65 | Oracle-grounded generation uses declared recipes held in the rules data, one per generated entity type. The AI requests a recipe by name, the server rolls the set, the AI interprets. Closes §4's open detail. | 9 | Approved |
| D-66 | Milestone 1's relevant-moves panel uses zero situation flags: a move is highlighted when its category is Session, Adventure, Quest, Fate, Suffer, or Threshold, verified as broadly applicable with no fictional-positioning requirement across every move in those categories. The situation-flag mechanism (moves setting/clearing flags other moves require) is real infrastructure with an empty Milestone 1 ruleset, first needed by Milestone 2's Enter the Fray. | 9 | Approved |
| D-67 | A Pay the Price result that maps to no suffer move applies no mechanical effect. The result grounds the AI's narration, and the AI may propose a clock or an entity under the authority it already has. | 9 | Approved |
| D-68 | A Pay the Price "roll twice" result recurses to a depth of two; a nested repeat is rerolled rather than recursed. Both results appear as their own oracle chips. | 9 | Approved |
| D-69 | The oracle reroll cap (D-29) counts per individual roll. | 9 | Approved |
| D-70 | The AI rerolls without interrupting the beat. The discarded chip stays visible and struck through with its stated reason, and the player may correct or reinstate it afterward under §3. | 9 | Approved |
| D-71 | Milestone 1 builds the scene data model and binds the scene header to it, but a session opens one scene and stays in it. AI-proposed scene transitions (D-43) move out of Milestone 1. | 9 | Approved |
| D-72 | The golden session runs against a committed fixture event log for session 1, so the recap is genuinely built from events. | 9 | Approved |
| D-73 | A corrected narration passage replaces the original in the rendered log, marked with an affordance that opens the original and the player's note. Both are retained as events. | 9 | Approved |
| D-74 | Momentum implements the full rule: maximum is 10 minus marked impacts, reset is 2 minus marked impacts, both projected from character state. | 9 | Approved |
| D-75 | Each AI call appends an event carrying its input and output token counts. The session token counter is a projection over those events, so it survives reload and resumption. | 9 | Approved |
| D-76 | D-23 confirmed: combat moves are Guided and ship in Milestone 2; Reference in Milestone 1. | 9 | Approved |
| D-77 | D-30 confirmed: characters are created before the session, on each player's own time. | 9 | Approved |
| D-78 | Momentum's fixed floor is −6, unaffected by marked impacts. Only the maximum (10 minus impacts) and the burn-reset value (2 minus impacts) are impact-reduced, per D-74. | 10 | Approved |
| D-79 | Momentum's impact-reduced maximum and reset value are floored at 0 rather than going negative, completing D-74's formula for the case D-74 didn't specify. | 10 | Approved |
| D-80 | Face Danger's weak-hit "make a suffer move (-1)" is not auto-chained to a specific suffer move — the text doesn't say which, and choosing between Endure Harm and Endure Stress is a fictional judgment the authority model already gives the player. | 10 | Approved |
| D-81 | Endure Harm's miss-branch compounding requirement (if health is already 0, also mark wounded or permanently harmed, or roll the oracle) is deferred past Milestone 1. Endure Harm's automation covers the common case; this rare compounding state waits for impact-marking to exist. | 10 | Approved |
| D-82 | Ask the Oracle's "pick two" is folded into the same five odds-tier options as "ask a yes/no question," rather than modelled as its own two-envisioned-options flow — mechanically it is the same "rate one likely and roll" action once the two options are stated. | 10 | Approved |
| D-83 | Voiding an event cascades over recorded causation: the causal subtree is computed and stored on the void event at write time, previewed before the player confirms, and the void is refused when a non-voided event outside the subtree references an entity or track introduced inside it. Void means "this shouldn't have happened"; amendment (A15, A16) means "this happened and was wrong." | 11 | Approved |
| D-84 | Void is limited to events in the current session. | 11 | Approved |
| D-85 | Token accounting is exempt from void: an AI call inside a voided cascade still counts toward the session token total (D-75), because the tokens were spent whatever the fiction now says. | 11 | Approved |
| D-86 | Entity amendment and void-reinstatement are both deferred past Milestone 1. They are coupled: D-70's reinstating of a discarded oracle roll implies amending the entity built from the surviving result. A9 requires only that the discarded chip stay struck through, so Milestone 1 ships display-only rerolls. This leaves §3's "correcting or retconning any AI-established fact" undelivered in Milestone 1 — narration correction (A15) and manual mechanical override (A16) ship; entity retcon does not. | 11 | Approved |
| D-87 | The starship is a display-only entity in Milestone 1. No golden-session beat mutates ship state — Beats 4 and 5 use the *Lantern Wake*'s sensors, which resolves as a character move. Whether a ship is an entity, an asset, or a character defers to Milestone 2. | 11 | Proposed |
| D-88 | The play-screen shell (5.1, 5.2) moves ahead of the creation and campaign-setup UI (3.2, 3.4, 4.2–4.5), because those have no React app to live in. D-57's build order otherwise stands, and the server-side halves of groups 3 and 4 keep their place ahead of it. | 11 | Approved |
| D-89 | Character creation assets are three slots, not a quota: **exactly two base paths**, plus one **final** asset which may be a module, support vehicle, companion, or another path — so a character ends with two or three paths. Deeds cannot be chosen at creation. The command vehicle (starship) is a default asset granted at creation and does **not** occupy a slot; its ownership (sole, shared, or another character's) is narrative only and is deliberately not modelled, having no effect on how the asset is used in play. Encoded as a data-driven spec in `rules` keyed to asset category IDs, each constraint traced to Datasworn's imported collection description where one exists (command vehicle p.55, paths p.56, deeds p.57) and cited to Rulebook pp.104–110 otherwise — those pages are outside the CC-BY subset, so they are cited, never quoted. | 11 | Approved |
| D-90 | Character-creation validation is pure and lives in `rules`, not on the server, because both sides need it: the client validates a field as it is edited, and the server revalidates on write because it is authoritative and a client is not to be trusted about the rules. One implementation, so the two cannot disagree. | 11 | Approved |
| D-91 | D-89's constraints **block** rather than warn: every one of them is explicit in the source, and the design record only warns where a rule is a recommendation. Two qualifications. Slot counts are judged only on a *complete* set, so a half-filled draft is in progress rather than invalid. And a path's `requirement` ("If you wield a bladed weapon…") gates *use*, not selection, so it is displayed and never blocks a choice — unlike a deed's, which gates acquisition. | 11 | Approved |
| D-92 | Starforged's starting stat array (3/2/2/1/1) and starting momentum (+2) are hand-authored constants in `rules`, cited to the rulebook rather than quoted, because Datasworn ships the data a character is made of but not the procedure for making one. Meter starting values and their bounds are read from the imported data, which does carry them. | 11 | Approved |
| D-93 | `vow.sworn` is dropped from the event catalogue: `track.created` with `kind: 'vow'` already says it. A vow carries the character who swore it (optional `characterId`), and `CharacterState.vowTrackIds` lists them, so a sheet need not scan every track in the campaign. | 11 | Approved |
| D-94 | Milestone 1 adds a new task, **5.0 HTTP read API**, ahead of 5.1: read-only endpoints (list campaigns, campaign state, narrative log page) that give `web` its first server round trip. Task numbers stay stable (D-88); command endpoints land with the first task that writes through them, not with 5.0. | 12 | Approved |
| D-95 | The read-model interfaces (`CampaignState` and its parts, `NarrativeLog`/`NarrativeBeat`/`NarrativeEntry`/`VoidMark`/`ResolvedNarration`) move from `server/src/projection/` to `shared/src/read-models/`, so `web` can import them. The folds that produce them (`project`, `buildNarrativeLog`) stay in `server/projection`, behind its existing purity lint fence — only the shapes move, not the logic. The client renders server-projected state in Milestone 1; it does not run the projector itself. | 12 | Approved |
| D-96 | `web`'s dependency set for the play-screen shell: keep `@tanstack/react-query` for server-state caching and cursor paging; drop `zustand` (never imported; local UI state is small enough for `useReducer`); add no router, CSS, or component-primitive library (a small hand-rolled route matcher, CSS Modules with custom-property tokens, and native `<dialog>`/`popover` cover Milestone 1's five routes and its drawers); add `eslint-plugin-react-hooks` as a dev dependency. | 12 | Approved |
| D-97 | Desktop/laptop (D-44) means a minimum supported viewport of **1280×720**. Rails never scroll (D-40); a rail section that would overflow at that size truncates to "+N more" and opens a drawer instead. Crew has layout priority in the left rail; clocks, vows, and progress tracks in the right. | 12 | Approved |
| D-98 | A crew mini-card's click opens the character drawer, as §8 already says. Choosing which character is acting (golden session Beat 3) is a control in the action composer (task 6.2), not the card click; the acting character is indicated on its card. | 12 | Approved |
| D-99 | The top bar's "connection status" (§8) means API reachability in Milestone 1, read from query error state — there is no realtime channel to report on (§9 defers it to Milestone 3). AI-provider availability is a separate indicator added by task 7.11 (D-53). | 12 | Approved |
| D-100 | Campaign setup (group 4) and character creation (3.2–3.4) are full-page routes outside the play screen, not drawers over it. D-30/D-77 already put creation before the session begins, so §8's "play never navigates away" doesn't apply to them. Opening a campaign lands on the play screen (A1), which is the campaign's home. | 12 | Approved |
| D-101 | Task 4.4's AI-proposed inciting incidents wait on the AI provider (group 7), which the work order runs after group 4. Task 4.4 builds only the player pick/edit/write-your-own path and the vow it produces now; the AI-proposal half of D-34 lands on the same form once 7.1/7.2 exist, the same treatment task 3.3 already gets. | 13 | Approved |
| D-102 | Task 4.3's oracle-grounded sector-location generation (D-32) waits on both the oracle-recipe API (task 8.1, where a `location` recipe would actually be declared — none exists yet) and the AI provider (group 7). Task 4.3 builds a manual location-list-and-routes editor now — the player writes each location's name and description directly, no roll involved — and swaps in oracle-grounded generation once 8.1 and the AI both land. | 13 | Approved (generation deferred past Milestone 1 by D-144) |
| D-103 | A route between two sector locations is its own event type, `sector.route_added: { fromLocationId, toLocationId }`, rather than encoded into `entity.established`'s per-entity `fields` bag. Locations themselves need no new event — they reuse `entity.established` (`kind: 'location'`) unchanged. | 13 | Approved |
| D-104 | Task 5.7's moves drawer is built in group 5, not deferred to task 6.1: a full moves reference browser (list grouped by category, click through to trigger/outcome text), reachable via a small "Moves" button added to the top bar. Task 6.1's "full list one click away" (`milestone-1.md`) links to this same drawer later rather than building a second one. NPCs, trackers and assets all had a real trigger already (an entity card, a rail row, an asset chip); moves did not, since the relevant-moves panel is task 6.1 — this is the one place group 5 adds a small seed entry point rather than leaving the drawer unreachable until group 6. | 14 | Approved |
| D-105 | Character creation (3.5) writes the new character and its optional background vow as **one command**, not two: `createCharacter` pushes both `character.created` and `track.created(kind: 'vow')` into the same `appendCommand` call. They are one decision, not two facts — a character created without the vow it was conceived around is not a state worth being able to reach, and a partial failure should leave neither event written. `backgroundVow` is optional on the request; a character can also be created with none and swear a first vow in play. | 15 | Approved |
| D-106 | Group 6's write API (`invokeMove`, `applyMoveChoice`, `burnMomentum`, `resolvePayThePriceMethod`) had no task number of its own in `milestone-1.md` — it lands as foundational work alongside task 6.2, the first UI that needs it, the same way D-94 covered group 5's read API. It follows the established command pattern (validate against the projection, mint no client-trusted values, `appendCommand`, typed rejection to 422) with one new wrinkle: a cross-command reference a client must supply (`chainedFromCommandId`, following an offered or auto chain to a further move) is the *commandId* the client already minted for the earlier call, not a server-issued event id — an event's id does not exist yet at the point a same-command payload would need to embed one (`event-store.ts` mints it at append time), and the client already has its own commandId in scope from having made that call. The server checks the named command actually wrote a `move.chained` naming the move now being invoked before accepting it as `causedBy`, so a client can follow a real offer but not forge one. | 16 | Approved |
| D-107 | Endure Harm's harm intake (A13, D-16) has no AI to propose an amount yet — group 7 (the AI provider) runs after group 6 in the work order. The composer shows a deterministic placeholder (the `proposed_amount` effect's declared range's midpoint) labelled explicitly as a placeholder, never as the AI's words, which the player adjusts before committing; the server logs only `amount.committed` (the player's own fact), not a fabricated `amount.proposed` — the design record's own event catalogue (`design-event-log.md` §4) already keeps these as separate types for exactly this reason. Group 7 adds a real `amount.proposed` and wires the placeholder over to it additively, the same treatment D-101/D-102 give an AI-proposal half that isn't built yet. The commit is bundled into the same command as the roll it precedes (one player decision — "I'm doing Endure Harm with this harm amount, now roll" — not two), so `amount.committed` carries no cross-referencing id either, for the same reason `move.chained` doesn't (D-106). | 16 | Approved |
| D-108 | `oracle.rolled` (designed in `design-event-log.md` but never written until now) is introduced by task 6.8 for Pay the Price's table method (Beat 7), not by group 8's broader oracle-grounded generation — group 6 is simply the first real caller that needs a server-rolled oracle result, ahead of group 8 in the work order. Its shape (roll value plus row text) is left exactly as designed and is reused unchanged when group 8 rolls NPC/location/faction recipes and AI-set odds, matching the precedent `truth.set` and `sector.route_added` already set: a type lands with the feature that first needs it. | 16 | Approved |
| D-109 | `Condition` (a `ChoiceOption.available` guard — only Endure Harm's `not: hasImpact` is exercised in Milestone 1) is evaluated by a new pure function in `rules/automation` (`evaluateCondition`), not in the server command layer. It takes small caller-supplied facts (`hasImpact`, `meterValue`) as parameters rather than reading campaign state itself, the same pattern `resolveActionMove` already uses for `markedImpacts: number` — `rules` still sees no campaign state (section 3's convention), it just receives the two facts a `Condition` can ask about. The full `Condition` union is handled, not just the one case Milestone 1 exercises, so a future guard doesn't silently fall through unhandled. | 16 | Approved |
| D-110 | Group 7's own demonstrable surface is `role: 'beat'` narration after a move flow ends, plus narration correction, the token counter and the graceful stop. Complication options (8.7), AI oracle rolls and chips (8.1–8.5), the recap (9.1), the summary (9.4), "What now?" (9.3) and `scene.header_updated` (no task) stay out, and `narration.written.groundedIn` is always `[]` until 8.x. Narration is requested when a move flow reaches **Done**: the client names the flow's commandId, the server walks `causedBy` up to the root move command and takes that command's causal subtree (the same computation a void cascades over), so one passage narrates a whole chain — Face Danger miss → Pay the Price → Endure Harm, with its choices and burns. The passage's `causedBy` is the latest live event in that subtree, so voiding any part of the chain voids the narration too (D-83), and causality is still never client-supplied (D-106). The request is refused when the named command is voided or the subtree already holds a `narration.written`. | 17 | Approved |
| D-111 | Narration streams as a `POST` whose response body is `application/x-ndjson`, read on the client with `fetch` and a stream reader. No WebSocket and no EventSource: D-99 defers a realtime channel to Milestone 3, and EventSource cannot POST. Frames are `delta`, `reset` (a retry discards the partial text), `committed { eventId }` and `failed { errorKind }`. Refusals are ordinary 422 JSON returned before any stream opens. Generation runs to completion even if the client disconnects, and the final text is committed as one command (`narration.written` plus `ai.completed`) when the stream ends; replaying a committed commandId returns a single `committed` frame. | 17 | Approved |
| D-112 | The AI provider interface (task 7.1) has two operations: `streamText` for prose and `generateStructured` for a JSON value validated against a zod schema. No tool use in Milestone 1's group 7 — task 8.1 chooses between tool use and a two-phase roll-then-narrate call for recipes, and either extends the interface additively. | 17 | Approved |
| D-113 | `ai.completed` gains optional `cacheReadTokens`, `cacheWriteTokens` and `firstTokenMs`, since a provider's uncached input count alone under-reports a cached call (optional fields need no version bump). A new event type, `ai.failed { provider, model, purpose, errorKind, message, attempts, inputTokens?, outputTokens? }`, with `errorKind` one of `unavailable`, `auth`, `rate_limited`, `refused`, `invalid_output`, `not_configured`, `rejected` (the provider refused the request itself as malformed — a configuration fault Retry cannot fix), records a call that produced nothing usable. Every attempt writes its own accounting event, retries included, and the session token counter sums both types (D-75, D-85). | 17 | Approved |
| D-114 | Narration latitude (D-07) is defined for all three settings. **Minimal**: the consequences of the declared action, plainly, with no embellishment. **Color**: style and sensory detail on the declared action, never a character's thoughts, feelings or dialogue. **Full voice**: Color, plus a character's spoken lines and outward expression where they fit the declared action. No setting lets the AI narrate a character's thoughts or feelings or decide anything for them (§3). **Amended, round 20:** outward expression is Full voice only, and Color says so rather than leaving it implied. The boundary of a player-owned interior is D-129's: sensation in the present beat, arising from something committed, is allowed at every setting; thoughts, emotions, intent, motivation, and any claim that reaches beyond the beat are not. | 17 | Approved (amended round 20, D-129) |
| D-115 | Narration length (D-11) is a word range from the beat's weight, scaled by the campaign's `narrationLength`. A **routine** beat is 60–120 words (§12); a **dramatic** beat — a miss, a match, a momentum burn, or a chain into a suffer move — is 120–200. `shorter` scales both ranges by 0.6 and `longer` by 1.5. Weights for scene openings, recaps and summaries land with the groups that write them (8.x, 9.x). **Amended, round 20:** a beat with **no declared action** is narrated at the routine range even when it would be dramatic. The extra length is where the round-20 violation came from, not just padding the beat didn't need. An Endure Harm with a match and nothing declared gave the AI six mechanical lines and asked for 120–200 words. It filled the room the player hadn't authored with an undeclared next step and a thirty-year disposition (D-127). Less player-written material gets less room. | 17 | Approved (amended round 20; scene-opening weight set by D-141) |
| D-116 | D-53's "the session stops" means **play pauses**. When a provider call fails after the SDK's own retries, or output fails validation after one re-ask, the server appends `ai.failed`; the play screen shows the Guide as unavailable, disables the action composer, and offers Retry. Committed state is never touched. A server with no API key configured starts in the paused state. | 17 | Approved |
| D-117 | A16's manual override — which had no task number, and whose command (`overrideState`) had no HTTP route or UI — lands with task 7.9, because Beat 9 exercises it alongside narration correction: a `POST /campaigns/:id/overrides` route, and edit controls on meters, momentum and track ticks, with player-overridden fields marked in text, not color alone. Same precedent as D-94 and D-106. | 17 | Approved |
| D-118 | `amount.proposed { moveId, characterId, meter, amount, reason }` lands in group 7, as D-107 anticipated, as structured output's first caller: when Endure Harm's harm intake opens, the AI proposes an amount within the effect's declared range with a one-line reason, replacing D-107's placeholder. The player may commit any amount without waiting for it, so dice still never wait on the AI. | 17 | Approved |
| D-119 | The Claude implementation defaults to `claude-opus-5`, overridable by `ASTROLABE_CLAUDE_MODEL`; `ASTROLABE_AI_PROVIDER=stub` selects the scripted stub (D-60). Beat narration runs with adaptive thinking at effort `low`, measured against A18's five-second target. Server-side refusal fallbacks are enabled, so a declined request is re-run on a fallback model inside the same call rather than stopping play. | 17 | Approved |
| D-120 | D-14's AI move suggestion gets a task, **7.12**, in group 7 because it needs the provider. It had none: A3 covers only picking from the panel, and Beat 3's "a player who typed the action without picking a move would get a suggestion instead" was an expectation with no task behind it. Submitting a described action with no move chosen is the request (D-10). A suggestion must meet the non-negotiable that automated behaviour is traceable to the rule entry behind it. It carries the suggested **move ID** and roll option, the **verbatim trigger text** it relies on, a one-line **reason**, and a **confidence**. The trigger text is the move's `trigger.text` or the chosen option's condition text, checked server-side with `isVerbatimClause`; a quote that fails the check is invalid output and is retried (D-113, 7.5). Confidence is a bounded scale, shown to the player and never read by anything mechanical. The player can open the suggestion to see the quoted trigger, the reason and the confidence. Accepting it fills in the move; it never invokes one. It never blocks picking a move directly, and the composer stays usable while it is in flight (the D-118 precedent). The event type and the confidence scale are proposed with 7.12's plan. | 18 | Approved |
| D-121 | D-37's trigger-mismatch note gets a task, **7.13**, in group 7. It had none. After a move is invoked with described action text, the AI may note that the move's trigger doesn't fit the action. The note carries the same traceability as D-120: move ID, verbatim trigger text, reason, confidence. It lands on the beat after the roll: it never blocks or delays the roll (A18) and changes no state. The player may ignore it, or void and redo. **Scope flag:** no golden-session beat exercises D-37. In Beat 7 the player catches his own +edge mistake, and the AI notes nothing. D-37 is included at the user's direction as a §3 authority obligation; the golden session is not amended. Open for 7.13's plan: whether the chosen stat's condition text counts as trigger text. The imported schema models it under `trigger.conditions`; if it counts, Beat 7's +edge roll would carry a note. | 18 | Approved |
| D-122 | The dev database and the end-to-end test (10.4) share one **fixture** mechanism rather than two. A fixture is a script that plays a campaign through the real command functions (`createCampaign`, `createCharacter`, `invokeMove` and the rest). Campaign and command IDs are derived from the fixture's name, and dice come from a loaded `RandomSource` that fails if the rules draw more dice than the script supplies. It lands in whatever state the commands produce, so a fixture can't describe a state the app couldn't reach. Session 1's passages go through beat narration with a scripted stub provider. Only `session.began` and `session.ended` are appended directly, because 9.1 and 9.4 haven't built their commands yet. The result is deterministic up to server-minted event, character and track IDs and timestamps; assertions compare by names and values. `session-1` is D-72's fixture: the Lantern Wake campaign as the golden session's Setup finds it, with session 1 played and ended. `session-2-open` is the same campaign under its own ID, with session 2 already open at the relay, so a person has something to play until Begin a Session (9.1) exists. **Reset** is drop-and-replay, never deletion. The events table stays INSERT-only, and nothing weakens its triggers: `npm run db:reset` drops and recreates the database's `public` schema, migrates, and seeds, and it refuses without `--yes` and under `NODE_ENV=production`. `npm run db:seed` adds a fixture only if its campaign doesn't already exist. `npm run harness` runs in a throwaway schema, so it no longer adds a campaign to the dev database on every run. **Round 21:** session 1's passages are scripted as D-127 segments and pass its checks. Writing them as segments showed the originals broke D-129: they gave Juno a disposition ("works it with a salvager's patience"), and at Color they voiced Rook's and Juno's opinions of Vesna's flying. Those words are gone, so the fixture text no longer matches earlier drafts of session 1's passages. | 18, 21 | Approved (round 20: settled as built, because 10.4 builds its end-to-end test on these fixtures) |
| D-123 | Concept-first creation (3.3) grounds invented detail the `setTruth` way: the server rolls and the AI interprets. Before the AI is asked, the server rolls given name, family name, callsign and two backstory prompts (`oracle:characters/name/*`, `oracle:campaign-launch/backstory-prompts`) and records them as `oracle.rolled`. The AI picks, combines or adapts those results. Each proposed name, callsign and hook cites the rolls it used, and a citation to a roll that wasn't made counts as invalid output. The AI invents no other named detail (§4, §6). This is not 8.1's AI-facing oracle API; the tables are fixed by the task, not chosen by the AI. | 19 | Approved |
| D-124 | A character proposal is an event, and nothing becomes canon until the player accepts it (§6). One proposal command writes the rolls, the AI accounting, and `character.proposed`: each field (name, callsign, stats, assets, background vow, hooks) with a one-line reason and the rolls it cites. Accepting goes through the unchanged `createCharacter` path. The server resolves the proposal from a `proposalCommandId` and records it as the character's `causedBy`, never taking causality from the client. `character.created` gains an optional `hooks` field; being optional, it needs no version bump. The proposal is checked with `validateCharacterDraft` before the player sees it, and a failing one is retried with the problem stated. The proposal plumbing is shared, so 4.6 is its second caller. `grantCommandVehicle` stays unused. | 19 | Approved |
| D-125 | The projection keeps a campaign-level token total, counting every `ai.completed` and `ai.failed` whether or not a session is open, alongside D-75's per-session counter. Characters are created before any session (D-77), and the session counter skips pre-session calls, so creation spend would otherwise be invisible. The top bar keeps showing the session count, with the campaign total in its tooltip. | 19 | Approved |
| D-126 | 4.4's AI-proposed inciting incidents (D-34, D-101) had no open task, because 4.4 was ticked with only the player-written path built. They become task **4.6**, built on 3.3's proposal plumbing (D-124) and worked right after 3.3. The incident table `oracle:campaign-launch/inciting-incident` already exists in the imported data. | 19 | Approved |
| D-127 | Beat narration streams as **segments**, not free prose, so a passage can't cover ground the player never authored without saying so. Found live (round 20): an Endure Harm played with no declared action was narrated at 120–200 words, and the AI gave Rook an undeclared next step ("steps sideways through the gap into Varga Relay") and a disposition with a history ("folded and stowed the way everything has been folded and stowed for thirty years"). The rules against both were already in the prompt. Each segment carries `about` (`world`, `character_undergoes`, `character_does`, and `character_says` at Full voice only), the `character` it concerns, a `basis` citing the beat's fact keys, and its `text`. `describeBeat` gives every fact a key and a kind: declared action, effect, roll, choice or injury. The tags stream before the text, so each segment is checked before its text reaches the player. The checks need no AI: a `character_does` segment must cite a declared action by that same character; a `character_undergoes` segment must cite a fact about that character; every cited key must exist; a `world` segment must not name a player character; and below Full voice a character segment must not quote speech. `character_does` stays in the schema even when nothing was declared. Removing it would let constrained decoding relabel the action as `character_undergoes`, hiding the violation instead of rejecting it. What this can't catch: a mislabeled segment, a player-owned interior written as undergoing, and a player character referred to only by pronoun in a `world` segment. D-128's check covers those. The committed `narration.written` text is the segments joined; an optional `segments` field keeps each basis resolved to event ids. Streaming structured output is an additive provider operation, as D-112 anticipated. If Claude doesn't stream the tags ahead of the text, the fallback is tagged segments over `streamText`, parsed as they arrive. **Amended, round 21 (as built in 7.14):** (1) **All four labels at every latitude.** `character_says` is offered at every latitude and rejected below Full voice. This is the same argument as for `character_does`: dropping the label would relabel speech rather than reject it. (2) **Constrained keys and callsigns.** `basis` is an enum of the beat's fact keys and `character` an enum of the campaign's callsigns, so constrained decoding enforces "every cited key exists" (**corrected, round 23:** it does not. `@anthropic-ai/sdk` 0.125's `betaZodOutputFormat` passes every `enum` and `const` to the model as description text, so no enum is a decoding constraint. The zod check afterwards, with one re-ask, is the only guarantee (task 8.1 notes). `checkSegmentTags`'s cited-key check is what enforces it). (3) **A `move` fact kind.** Facts gain the kind `move`, for which move was made and what it chained into. (4) **Prompt wording.** The prompt's `world` definition excludes a player character's body, gear and blood. Its `character_undergoes` definition names the verbs it excludes (moves, braces, breathes deliberately, tests, shakes, holds, decides, reacts). Without those verbs, the spike labelled undeclared actions as undergoing in 5 runs out of 5. The name check itself is unchanged. (5) **The fallback is moot.** Structured output streamed the tags first in every run, and first checked text arrived in 1.3–3.0 s. (6) **Honest labelling is only half evidenced.** When a declared action made `character_does` legal, the model used it and cited the fact in 4 runs out of 4. No run ever wrote an undeclared action for it to label as one. So "a mislabelled segment" in the list of what this can't catch is unrefuted, not hypothetical. (7) **Undeclared action in a permitted label.** An undeclared action inside a label that is allowed also passes these checks. Recorded live: "as the arm comes back up to the plate" in a Full-voice `character_says` segment. D-128's checker must judge action in every label, not only mislabelled undergoing. | 20, 21 | Approved (amended round 21) |
| D-128 | Every beat narration, narration rewrite (7.9) and harm-proposal injury (D-130) is **checked before it commits**, and a failing passage is withdrawn, never quietly rewritten. A second, faster model (`ASTROLABE_CHECK_MODEL`, default `claude-haiku-4-5`) gets D-129's rubric for the campaign's latitude, the beat's facts, and the tagged segments. It returns violations, each with the segment, a verbatim quote, the rubric rule and why. The server confirms each quote is verbatim; an unverifiable verdict is re-asked once, then fails closed. Text streams **provisionally** while it is generated and checked, so A18 is still measured on the first attempt. Holding the text until checked would put first text past ten seconds. A **withdrawal** — a D-127 segment check or a checker verdict failing — is **unmistakable**. The passage is visibly struck from the log with its reason in words ("Withdrawn: it said how Rook characteristically responds, which is the player's to decide"), and a `narration.withdrawn { attempt, checker, latitude, rejectedText, violations }` event records it. That event is narrative, so the withdrawal stays in the log, with the rejected text a click away. The re-ask carries the quoted violations. A second failure commits nothing but the withdrawal and `ai.failed` (`invalid_output`), and play pauses under D-116 with Retry as the one way on. A checker that is unavailable fails closed the same way: an unchecked passage is never committed. The checker's tokens land on `ai.completed` with purpose `narration_check`. The checker judges only D-129's rules and D-130's injury, not general consistency with committed mechanics: every extra rule costs precision, and a false positive pauses play. Verdicts are not unit tested (§10). Recorded live verdicts replay through the stub in CI, and a keyed eval (`npm run eval:authority`) re-grades the recorded corpus whenever the rubric or checker model changes. **Amended, round 21 (before building 7.15):** (1) **The default checker model can't take the narrator's request.** A live probe showed `claude-haiku-4-5` rejects both adaptive thinking ("not supported on this model") and `output_config.effort` ("does not support the effort parameter"). It accepts structured output and the fallback beta, and answers in about 1 s. The Claude implementation therefore knows per model whether to send thinking and effort. That stays inside `claude.ts`; D-112's interface doesn't change, and a caller never passes model knowledge. (2) **Every checker failure fails closed and pauses play.** The failure is `ai.failed` under the checker's name and model, with the checker's own error kind. A `rejected` request, meaning a misconfigured `ASTROLABE_CHECK_MODEL`, reads as a server configuration problem rather than an outage, and Retry works once it is fixed. (3) **Input shape.** The checker takes the passage's text, plus its tagged segments when there are any. Beat narration has segments. A 7.9 rewrite streams as prose, and an injury is one sentence. (4) **Violation shape.** Each violation carries a `rule`: `undeclared_action`, `player_interior`, `voice` or `injury`. It also carries the `character` it concerns, the segment index when there is one, the verbatim quote, and `why`. A quote counts as verbatim when it appears exactly in that segment's text, or in the passage's text when there is no segment. The undeclared-action rule judges the whole passage, whatever the labels: an action inside a Full-voice line of speech is still an action (D-127's amendment). (5) **Withdrawal record.** `narration.withdrawn` also records the `role` (`beat`, `revision` or `injury`) and, for a revision, the `targetEventId` it was rewriting. `checker` is `segment_checks` or `authority_check`, and an authority check also records the checker's `model`. A segment-check failure's violation has the rule `segment_check`, with its problem in words. A checker failure over text that was already shown is withdrawn with the rule `unchecked`, because provisional text is never quietly removed either. (6) **Cut-off and unreadable output.** A reply cut off at `max_tokens`, or output that won't parse, is not an authority finding. It keeps 7.5's silent `reset` and re-ask. (7) **One command.** The withdrawals are written in the same command as the outcome, alongside every attempt's `ai.completed`, the narrator's and the checker's. The client sees each withdrawal the moment it happens, as a `withdrawn` frame carrying the reason and the rejected text. A `checking` frame marks when generation ends and the check begins. (8) **Unsaved proposals.** A harm proposal isn't shown until checked, so a failing injury is re-asked without anything to strike on screen. It is still recorded as a withdrawal with `role: 'injury'`, so the automated rejection stays traceable. (9) **The reason in words** comes from the rule and character by one shared function, so the frame and the log say the same thing. (10) **Live matrix.** The matrix uses five beats that can be built today: Beat 7's chain, a standalone Endure Harm with nothing declared, a Gather Information weak hit, an aided scan upgraded by a momentum burn (Beat 5), and a Face Danger strong hit. Beats needing complications, oracle grounding or clocks wait for groups 8 and 9. (11) **Default checker: `claude-sonnet-5`, not Haiku (measured in 7.15).** On the 26-entry corpus, Haiku 4.5 with the first checker prompt had 100% precision but 36% recall: it passed a Color-latitude spoken line and every spike passage with an undeclared action. With a `review` field written before the verdict and a check procedure, Haiku reached 86% recall but 80% precision, at about 6 s a check. Sonnet 5 at low effort, over 3 runs, reached 95% precision and 86% recall, at 3.9 s average and 5.7 s p90. Both of its false withdrawals were the same borderline line; both of its repeated misses were actions a player-chosen "Shake it off" might be read as covering. D-128 prizes precision because a false positive pauses play, so Sonnet 5 is the default. `ASTROLABE_CHECK_MODEL` still overrides it, and D-128 amendment (1) keeps Haiku usable. The checker answers with `review` before `violations`. | 20, 21 | Approved (amended round 21) |
| D-129 | One **authority rubric** (`ai/context/authority-rubric.ts`) states the player-owned boundaries. The narrator's prompt and D-128's checker use the same text, so they can't disagree. **Undeclared action:** a player character does only what the player declared: no further step, no move to a new place, no deliberate reaction. With nothing declared, the character does nothing, and the passage narrates what happens to them and around them. **Player-owned interior:** a character's thoughts, emotions, intent and motivation belong to the player (§3). A sensation that happens in this beat and arises from something already committed is the world acting on the character, and is allowed ("a bright hot line through the undersuit"). The test is **duration, not observability**: forbidden is any claim that reaches beyond the beat — a disposition, a history, a value, or how the character characteristically responds ("the way everything has been folded and stowed for thirty years", "that is the whole of what matters"). Invented history is this same rule, not a second one. Observable-versus-experienced was considered and rejected: "the arm still closes" and "Rook feels the arm close" make the same claim, and that isn't what §3 protects. **Voice:** a player character's spoken lines and outward expression (a grimace, a steadied voice) are Full voice only, and never commit the character to a choice. D-114's Color and Minimal blocks say so explicitly. The rubric adds three rules to the Guide's standing prompt: nothing beyond the declared action, with the passage ending where the resolved outcome ends; with no declared action, only what happens to the character; and the interior rule as written here. They lower the violation rate; D-127 and D-128 are the guarantee. | 20 | Approved (undeclared action extended to the crew acting together by D-141) |
| D-130 | A harm proposal **establishes the injury once**, and the narration follows it (Beat 7: the proposal's "ruptured conduit sprays sparks across Rook's arm" carries into the passage, and only the severity changes). Found live: the harm reason and the passage invented two different wounds. The proposal answers `injury` (what physically happens and where, with no severity words) separately from `reason` (why this severity). `amount.proposed` gains an optional `injury`. `amount.committed` gains an optional, server-validated `proposalEventId` (same move, character and meter; not voided). It is a payload reference, not `causedBy`, because a standalone suffer move's proposal is written before the move exists: as `causedBy`, D-110's walk to the root move would climb into the proposal command and refuse the beat. `describeBeat` still leaves out the proposed *amount*, since the prose follows the committed one. It adds the injury as a fact D-127's segments cite and, when the player changed the severity, says so ("the player set it at 1, milder than the Guide's 2"). D-128's checker rejects a passage whose injury to that character differs from the established one. If the player commits before a proposal arrives, there is no injury fact, and the passage works from the committed amount. The player's disagreement with the injury fiction itself is handled by correcting the passage (A15); no beat exercises editing the injury. | 20 | Approved |
| D-131 | A character's **pronouns are stored on the character**, not guessed. Found live (round 20): with none recorded, the AI gave Rook "his" in two separate prompts, the harm reason and the passage. Guessing only in the prompt would recur every session, and reads as the app not knowing who the player's character is. The player has already decided. `character.created` gains an optional free-text `pronouns` (trimmed, up to 40 characters; optional, so no version bump), set by a creation input that offers common values and preselects none. The crew line in the AI's context names them ("Vesna Kade, called Vesna (she/her)"). When none are recorded, it says only "(pronouns not recorded)", which implies no default. The rule for that case — no pronoun at all, only the name or callsign — is stated once in the standing prompt rather than repeated inside state. A concept-first proposal (3.3) fills the field only when the concept states pronouns, and its check rejects a value whose words aren't in the concept, D-123's test for a name the player wrote. No golden-session beat exercises choosing pronouns; Beat 5's "her" for Vesna is prose, so the `session-1` fixture records Vesna as she/her and Rook and Juno as not recorded. It lands as task 3.6 because the gap shows up in every session's narration. Changing pronouns after creation is not built. | 20 | Approved |
| D-132 | 4.6's **incident proposal** is D-124's shape, as its second caller. One proposal command rolls `oracle:campaign-launch/inciting-incident` three times, one roll per option, and records the rolls (D-123). The AI answers three options. Each has a vow `title`, a challenge `rank`, a one- or two-sentence `situation`, one `reason` for the option as a whole, the rolls it cites (at least one), and what it `drawsOn`: the answered truths, sector locations and crew characters it builds from. It answers against schema enums of what exists, and those are resolved to ids in `incident.proposed` (**corrected, round 23:** the enums are not enforced while decoding. `@anthropic-ai/sdk` 0.125's `betaZodOutputFormat` passes every `enum` and `const` to the model as description text, so no enum is a decoding constraint. The zod check afterwards, with one re-ask, is the only guarantee (task 8.1 notes). `checkIncidentProposal` and the zod parse are what reject an unknown key). Two options may share a roll only if they take it in different directions. The AI invents no named people, places or factions (§4, D-123). It may use a name already established in the campaign, and the player can add names by editing. A failing answer is re-asked once with the problem stated. Swearing the vow stays `swearIncitingVow`, which gains an optional `proposalCommandId`. The server resolves the proposal and records it as the vow's `causedBy`, never taking causality from the client (D-124). The title and rank are whatever the player sends, edited or not. Write-your-own stays as prominent as the proposals, and a proposal is never required. Like a character proposal, the command belongs to no session and stays out of the narrative log, and its tokens count toward the campaign total (D-125). | 22 | Approved |
| D-133 | D-34 and §6 draw incidents from **the characters' backgrounds**, but the setup wizard reaches the incident step before any character can exist: characters belong to a campaign, and the wizard creates the campaign in its first step. The proposal therefore draws on whatever crew exists (name, callsign, pronouns, background vow, hooks), alongside truths and sector. With no crew it says so, and so does the step. The wizard gains no crew step, because the golden session doesn't exercise campaign setup and a resumable wizard would be new machinery. The crew path is exercised on the seeded crew. | 22 | Approved |
| D-134 | **Incident options are not sent through D-128's checker.** Nothing in a proposal becomes canon until the player picks it, edits it or writes their own (D-124). Character proposals, whose hooks propose a player character's history, aren't checked either. The prompt carries D-129's narrator rules, so an option describes a situation the crew faces, never what a character feels, wants or decides. The harm injury is checked (D-128 (8)) because it becomes an established fact the narration follows; an incident becomes a vow only by the player's own act. | 22 | Approved |
| D-135 | 7.12's **move suggestion**, as D-120 asked its plan to propose. **Request:** the player types an action for the acting character and asks for a suggestion without picking a move (D-10). The Guide gets the action text, the actor, projected state, and the candidate moves, each with its trigger text and roll options. **Candidates** are the moves the composer can play from a described action: those both automated and in the relevant-moves panel, minus session moves and Pay the Price, which is reached only by a chain. Today that is Face Danger, Secure an Advantage, Gather Information, Swear an Iron Vow, Reach a Milestone, Endure Harm and Ask the Oracle. **Answer:** a move or none, a roll option, the trigger quote, a one-line reason, and a confidence of `low`, `medium` or `high`. Three words give no false precision; nothing mechanical reads the rating. Move and stat are enums of what exists (**corrected, round 23:** enums the model is told about, not enforced while decoding; `@anthropic-ai/sdk` 0.125's `betaZodOutputFormat` passes every `enum` and `const` to the model as description text, so no enum is a decoding constraint. The zod check afterwards, with one re-ask, is the only guarantee (task 8.1 notes).) The server checks with `isVerbatimClause` that the quote appears in that move's `trigger.text` or the chosen option's condition text, and that the option belongs to the move. A failing answer is re-asked once, then recorded as `ai.failed`. **No fit:** when nothing fits, the answer names no move and gives a reason, and nothing is forced. **Link markup is moot:** no imported trigger text or condition text carries link markup (checked across all 56 moves, and pinned by a rules test). The quote is stored verbatim, and it is also what the player reads. **Record:** `move.suggested { actorCharacterId, actionText, moveId, rollOption?, triggerText, reason, confidence }`, where `moveId` is null when nothing fits. It is written in one command with `ai.completed`, in the open session and scene. It is not narrative and changes no state. **Using it** fills the composer (move, roll option, action text) and never rolls. `move.invoked` gains an optional `suggestionEventId`, which the server checks names a live `move.suggested` for that move and actor. It is a field, not `causedBy`: beat narration walks `causedBy` up to the root move (D-110), the same reason D-130 gives. **Never blocking:** the relevant-moves panel stays usable while a request is out, a move picked by hand carries the typed action into the composer, and a late suggestion is dropped. **No authority checker:** a suggestion is a rules judgement the player reviews and nothing in it becomes canon (D-134's reasoning). The prompt forbids adding to what the player described. | 22 | Approved |
| D-136 | 7.13's **trigger-mismatch note**, resolving D-121's open question. **Trigger text only:** the note judges whether the chosen move's `trigger.text` fits the described action, and never the stat. A roll option's condition text doesn't count: the stat is the player's call, and an unasked comment on it would be the nudge D-10 rules out. So Beat 7's +edge roll carries no note, as in the golden session. 7.12 quotes condition text to support a suggestion the player asked for; the two uses differ. **When:** after every invoked move whose player typed an action, the client asks in the background once the roll has returned (A18). Moves filled from a 7.12 suggestion are skipped, since the Guide already judged them. The server refuses a move with no action text, one filled from a suggestion, a voided move, one already checked, and anything that is not a move. **Answer:** `{ fits, triggerText, reason, confidence }`, with D-135's three-word confidence. When the move fits, only the accounting is written. A mismatch must quote the move's trigger text verbatim (`isVerbatimClause`, at least 12 characters), and a failing answer is re-asked once, then recorded as `ai.failed`. **Record:** `move.trigger_noted { moveId, actionText, triggerText, reason, confidence }`, written with its accounting in a command whose `causedBy` is the `move.invoked`, so voiding the move voids the note (D-83). It is narrative, shown with its move in the log, and changes no state. Beat narration leaves it out of the facts it narrates and of the passage's `causedBy`, so voiding a note never voids a passage. **The player** may ignore it or void and redo the move; there is no dismiss. **No authority checker**, for D-134's reason: a note is a rules remark the player reads, and nothing in it becomes canon. **Checked once:** a move is checked at most once, and a check that failed (an outage, or two unusable answers) is not retried. The note is optional help: D-37 promises the player may be told, not that they will be, and a retry path would be UI for a remark. | 22 | Approved |
| D-137 | Group 8's **oracle grounding is roll-then-interpret, not tool use**, resolving D-112's open choice. The AI names what to roll (a recipe, an odds tier, a roll to reroll) and never a result. The server rolls and records `oracle.rolled`, and a second call interprets, citing each roll as an enum key: D-123's device, proven live in 3.3 and 4.6. It is the trust boundary 6.3 set for stats: name *which*, never the value. Tool use was rejected because rolls landing mid-stream fight D-127's segment gate and D-128's check before commit, and a tool loop would be a new provider operation. D-112's interface is unchanged. | 23 | Approved |
| D-138 | A **world pass** runs before any passage that may generate world content: beat narration (Beats 6 and 8) and the scene frame (Beat 2, D-141). (1) A **plan** call (structured) may request recipes by enum, create clocks, tick existing clocks by enum, or ask yes/no world questions with odds (D-28). The prompt says most beats need none of these, and a plan that asks for nothing writes only accounting. (2) The server rolls what the plan asked for. (3) An **interpret** call keeps or rerolls each result with a reason, within the cap (D-142), then answers each entity: a name and one field per recipe slot, each citing its roll. (4) One **world command** commits the rolls, rerolls, entities, clock events and accounting. Its `causedBy` is the chain's latest live event, so voiding the move voids the world pass and the passage with it (D-83). Its commandId is derived from the narration request's, so a replay finds it. The stream sends the chips, the entity and the clock to the client before any prose. (5) Narration runs as before, and world results become facts it cites. The passage's `groundedIn` is the rolls behind the facts its segments cite, where a cited entity counts its own rolls. Those rolls render as chips under the passage rather than as rows of their own. Pay the Price's table roll keeps its row. The pass runs **once per chain**: a narration Retry reuses a committed world pass, so a retry never makes a second NPC. A plan or interpret failure pauses play under D-116, and rolls already made stay committed. **8.4 is minimal.** No golden-session beat exercises AI-set odds (Beat 6's contradiction is a reroll), so a yes/no question is a plan field, rolled on Ask the Oracle's odds tables and recorded with its question (D-142), and nothing more. It is included by D-28, as D-121 included D-37. **Open for the step-0 spike:** a generating beat makes three calls before first text, so A18 is measured on Beats 6 and 7–8 before anything is built, and that measurement decides which model plans and interprets. **Amended, round 23 (step-0 spike, `ai/eval/live-spike-8.json`):** the measurement decided the order, not the model. Run in sequence before narration, a beat that generated an NPC or derelict reached first text at 12.1–15.0 s: the interpret call alone took 6.9–8.0 s on Opus 5 and Sonnet 5. Haiku 4.5 was no faster (8.4–23.8 s), and a plan that asked for nothing still put first text at 3.4–6.1 s. So **the world pass runs after the passage, not before it.** The beat is narrated exactly as today, and A18 is unchanged. Once its passage commits, the client asks for the world pass in the background, as it asks for 7.13's trigger check. The plan reads the committed passage, so what it establishes can't contradict it. Anything established is narrated in a **follow-up passage** of its own: Beat 6's first contact over comms, or Beat 8's clock. Each passage then holds A18 on its own request, and the NPC card arrives some 20 s after Done. The world command's `causedBy` is the beat's passage, so voiding the move still voids the world pass and its passage (D-83). The spike also found three things. (a) The plan sees the move's outcome text: without "Envision what you learn", no run made Beat 6's NPC. (b) The derelict recipe is offered only to the scene frame: in 2 of 3 Opus runs, a beat pass requested it for Varga Relay, which is already established. (c) Clock creation needs a firmer rule than "most beats need nothing": a routine scan got a clock in 3 of 6 Opus and Sonnet runs; that rule is 8.6's (D-145). **Amended again, round 23 (user's choice after 8.4):** a yes/no question may name a recipe in `onYes`, rolled only if the oracle answers Yes, in the same pass and with no extra call. Its reason is the question it answered. Before this, a plan couldn't make a recipe depend on an answer. So when 8.4's live pass asked 'Is someone still alive aboard?' and got Yes, the follow-up passage narrated an unnamed voice with no NPC card. A No rolls nothing more and is narrated as it is. | 23 | Approved (amended round 23) |
| D-139 | **Recipes are hand-authored in `rules`**, the way automation specs are, because Datasworn ships none. Each slot names its table, and a test checks the table exists. Milestone 1 declares the two the golden session exercises. `recipe:npc` rolls role, goal, first look, initial disposition, given name and family name (Beat 6). `recipe:derelict` rolls condition, outer first look and inner first look (Beat 2). It has **no location or type slot**, which amends §4's example. `derelicts/type/*` depends on the location roll, which a flat slot list can't express. And Beat 2 frames Varga Relay, which is already established: rolling its location could contradict canon, and once the reroll cap was reached the AI would be stuck with the contradiction. Generating a *new* derelict waits for a beat that needs one. No location or faction recipe is built (D-144). | 23 | Approved |
| D-140 | **World text gets a check that needs no AI**, not D-128's checker. Entity names and fields, clock titles and reasons, reroll reasons, question text and complication options (D-143) must not name a player character, by 7.14's whole-word name check. A failure is re-asked once with the problem stated, then recorded as `ai.failed`. D-128's checker isn't used because none of this is prose about a character, and a checker call before narration would cost A18 several seconds (D-134's reasoning). What it can't catch: a pronoun, or "the crew", standing for a player character. Beat 8's reason, "forcing the bulkhead tripped emergency load-shedding", passes. | 23 | Approved |
| D-141 | The **scene frame** (Beat 2, `narration.written` with role `scene_frame`) is a command of its own, not beat narration: it follows no move, so `resolveBeatScope` doesn't apply. Its facts are the scene, its location, and the world pass's results. It streams as D-127 segments, **`world` segments only**. Nobody declared anything, so under D-129 no player character does anything, and the segment name check enforces that. It goes through D-128's checker with those facts. **Rubric amendment (D-129):** undeclared action covers the crew acting together ("the crew docks", "they move in"), not only one character. **Trigger:** until 9.1 exists, a "Frame the scene" control in the scene header while the open scene has no frame. 9.1 then asks for it after the recap. A scene has one frame. **Length (D-115):** a scene opening is weighted at the dramatic range, 120–200 words, scaled by `narrationLength`. D-115's routine cap for beats with nothing declared doesn't apply, because the cap's reason was room for undeclared action, and world-only segments leave none. The frame ends on the stakes and never suggests what to do (D-10). **Amended, round 23 (after 8.2):** the frame's plan call runs on a separate planner, `claude-sonnet-5` by default (`ASTROLABE_PLAN_MODEL` overrides it), while the passage stays on the narrator. With Opus 5 planning, first text reached the player at 4.9–6.3 s and missed A18 in most runs. With Sonnet 5 it reached 3.9–6.5 s over 5 runs, a median of 4.3 s, and only the first run missed, on a cold call (`ai/eval/live-frame-sonnet-plan.json`). Beat world passes keep planning on the narrator: they run after their passage, so A18 doesn't bind them. | 23 | Approved (amended round 23) |
| D-142 | A **reroll** (8.3; D-18, D-69, D-70) is written in the world command as a new `oracle.rolled` plus `event.voided { kind: 'reroll', cascaded: [the discarded roll] }`, appended directly. It does not go through `voidEvent`, whose cascade is command-level and would void the whole world pass. `oracle.rolled` gains three optional fields, so no version bump: `rerollOf`, the roll it replaces; `recipeId` and `slot`, the recipe slot it fills, for the chip's label and D-65's reproducibility; and `question`, for a yes/no world question (D-28). The cap is counted along the `rerollOf` chain, per individual roll (D-69). A roll at its cap is left out of the reroll enum, so the AI must work with it. The discarded chip stays visible, struck through, with its reason. Reinstating one stays deferred (D-86). | 23 | Approved |
| D-143 | **Weak-hit complications** (8.7; A5, D-15). Gather Information's weak hit is the only Milestone 1 outcome that calls for a complication and offers no menu. Its spec gains a traced `complication` marker ("but also complicates your quest"), so the move flow reads data rather than a move id. The result step asks "What complicates things?" and **requires an answer before Done**: §3 gives the complication to the player, or to the player's pick among AI options, never to the AI alone. The player writes one or asks for options. For options, the server rolls one Action + Theme pair per option (D-132's one roll per option), and the AI answers three, each citing its pair. They get D-140's check, not D-128's (D-134). `complication.offered { options: [{ text, groundedIn }] }` is the AI's, and `complication.set { text, source: 'written' \| 'offered', offeredEventId?, optionIndex? }` is the player's. Both have the `move.invoked` as `causedBy`. The set complication is a beat fact, and a picked option's rolls join the passage's `groundedIn`, so Beat 3's passage carries chips too (A2). **Amended, round 23 (user's answers before 8.7):** (1) **Pick, then edit.** Picking an offered option fills the complication box, and the player may change the words before setting it. `complication.set` records `source: 'offered'` when the words are unchanged, and `source: 'written'` with `offeredEventId` and `optionIndex` still naming the offer when they were edited. (2) **Ask again as often as needed.** Each request rolls fresh Action + Theme pairs and replaces the options shown. Every set stays in the log as its own `complication.offered` with its rolls. | 23 | Approved (amended round 23) |
| D-144 | **D-102's oracle-grounded sector locations are deferred past Milestone 1.** The golden session starts at session 2, and no beat generates a location or a faction, so 8.5 builds NPC entities only. `recipe:location` and a faction recipe wait for a beat that needs them. 4.3's manual location editor stands as Milestone 1's sector. | 23 | Approved |
| D-145 | **Clocks the Guide sets** (8.6; A14, §3's clocks, D-138, D-140), from the user's answers before building. (1) **Pressure beats only.** The world pass's plan is offered clock fields only when the beat has a miss, a match, or a Pay the Price chain. The server enforces this gate by leaving the fields out of the schema and refusing them in the check; within it, the prompt judges. Beat 8 follows Beat 7's miss and passes the gate, and a routine scan can never make a clock (the spike gave one to 3 of 6 routine scans). A scene frame sets no clocks. (2) **What the plan may do.** It may create at most one clock per pass: a title, 4, 6, 8 or 10 segments, how many start filled, and a one-line reason. It may tick at most two existing, unfilled clocks, named by key, each by at least one segment but never past full, with a reason. Each clock gets D-140's name check on its title and reason. The events are `track.created` (kind `clock`) and `track.advanced`, both caused by `ai_judgement` with the reason, in the world command. A clock that starts partly filled is created, then advanced by the same reason, so hovering shows who filled it and why (Beat 8). (3) **Shown, not narrated.** A clock appears on the pressure rail with its reason and a log row. A pass that only sets clocks writes no follow-up passage, and a follow-up passage about an NPC leaves the clocks out of its facts. (4) **A full clock** does nothing mechanical in Milestone 1. No beat exercises one. Later prompts still see it as full, and the player can override it (A16). | 23 | Approved |
| D-146 | **Session lifecycle is commands, not moves** (group 9; D-59, D-71, D-116). Begin a Session and End a Session don't go through `invokeMove`: their specs have no outcome automation, and what the beats test (the recap, the summary) isn't a rules effect. **Begin Session** (`session.begin`) writes `session.began` and `scene.started` in one command and commits at once. The recap streams afterwards (D-147), so state never waits on it, and a recap failure pauses play under D-116 while the session stays open. **The scene is carried forward:** the new scene copies the title and location of the previous session's last scene, under a new scene id and unframed, so D-141's Frame the scene applies. Begin Session takes no scene input. **A campaign's first session** has nothing to carry, so only then does Begin Session ask for a scene title and an optional location from the sector. No golden-session beat exercises it; it keeps a newly set-up campaign playable. **No second begin:** it is refused while a session is open. **No moves outside a session:** `invokeMove` and the AI commands that belong to play refuse with `no_session` when no session is open. **Fixture exception to D-71:** `session-1` starts a second scene at Varga Relay ("The derelict relay station") when the crew reaches the relay's sensor shadow, so session 2 carries it forward. Nothing in the app writes a second scene within a session. **The log is per session:** the narrative log reads the open session, or the latest ended one, as `design-event-log.md` §10 always specified. | 24 | Approved |
| D-147 | **The recap** (9.1; A1, D-12, D-72, D-127–D-129). One command, `narration.recap`, caused by `session.began`, writes `narration.written { role: 'recap' }` and its accounting. **Facts** are the previous session's `session.ended` summary and open threads, and its significant, non-voided events in `seq` order (§10's read path), each under a fact key. **Segment mode:** a recap retells what was already declared and committed, so it isn't world-only (D-141) and has no beat. A `world` segment may cite any recap fact. A `character_does` or `character_says` segment may cite only a `move.invoked` from that session carrying the player's declared action text, and may not go beyond that text. A `character_undergoes` segment must cite a fact about that character, as in a beat. D-128's checker runs as on any passage, with its withdrawal and one re-ask. **Length (D-115's recap weight):** routine, 60–120 words, scaled by `narrationLength`. No recap is requested for a campaign's first session, which has nothing to recap, and a resumed session gets no second recap. | 24 | Approved |
| D-148 | **"What now?"** (9.3; A6, D-10, D-134, D-135). On request only, one structured call answers **exactly three** suggested actions, each with `characterId`, `actionText`, `moveId` (nullable), `reason` and `anchors`, the keys of the rendered-state facts it builds on (a vow, a clock, an entity, an open thread, the scene). The server checks that each anchor exists, each character is in the crew, each move exists, and the three are distinct. One re-ask, then `ai.failed`, shown in the composer without pausing play. **Any move may be named**, including one at Reference (Beat 4's Undertake an Expedition). **Using a suggestion** fills the composer with its actor, move and action text and never rolls; for a move the composer can't play, it opens that move in the moves drawer and keeps the action text. **Record:** `actions.suggested { suggestions }` with its `ai.completed`, in one command. It is not narrative and changes no state. **No D-128 checker:** nothing in it becomes canon (D-134's reasoning). The prompt carries D-129's rules, so an action never includes a thought, emotion or intent. | 24 | Approved |
| D-149 | **End a Session** (9.4; A17, D-116, D-124, D-128). **Proposal, review, commit**, D-124's shape. (1) A structured call over the session's significant, non-voided events answers a summary (80–150 words, D-115's summary weight) and 2–5 open threads, each about the world, an NPC or a vow and never a character's intent. It is written as `session.summary_proposed` with its accounting. The summary goes through D-128's checker, because the next recap reads it as canon, and the threads get D-140's name check. (2) The player reviews and may edit the summary or the threads. (3) **End session** commits `session.ended`, naming the proposal, authored by the player when edited and by the AI otherwise. **The one lifecycle step that waits on the AI:** `session.ended` requires a summary, so a failed proposal pauses under D-116 and the session stays open with state intact. **Reach a Milestone (Beat 10)** is a reminder shown with the proposal for each open vow: "Reach a Milestone is available if you judge one was earned." It has no mechanism behind it in Milestone 1; the player marks progress by manual override (2.6). End a Session's "+1 momentum for a noted thread" stays unmodelled. **Amended, round 24 (as built in 9.4):** a failed proposal does not pause play globally. The End Session panel shows the failure with **Retry** and **Cancel**, and Cancel returns to play with the session still open. Nothing has been committed that needs the AI, so blocking the move flow as D-116 does would stop play for no reason. The session still can't end without a summary. | 24 | Approved (amended round 24) |
| D-150 | **Resuming a campaign** (9.5; §9's AI outage, §10's resume target). Three cases, nothing more. (1) **The last session ended:** the play screen opens with Begin Session. (2) **A session is open:** play resumes in place, with the current session's log, header and rails. (3) **A committed move chain with no passage:** the narration queue is client memory, so a reload drops it. The server reports the open session's chains still owed a passage, and the log offers **Narrate** on each; a chain still owed a complication shows its complication prompt first. A half-finished move flow (a choice not yet made) and a mid-session recap are not restored in Milestone 1. **Amended, round 24 (as built in 9.5):** the owed chains are returned by the state route beside `state` (`owedPassages`), not added to `CampaignState`, so the projection doesn't depend on beat scopes. They are computed on each state read by resolving the beat scope of every root move in the open session. That cost grows with the session's length; it is acceptable for Milestone 1's single player, and it is the first thing to bound if the state read slows. World passes owed after a passage (D-138) are not re-offered. | 24 | Approved (amended round 24) |
| D-151 | **Group 10's order and scope** (D-57, D-88). Group 10 is worked in the order **10.4 · 10.5 · 10.1 · 10.2 · 10.3**. Task numbers stay stable. The end-to-end test goes first because it is the only task in the group with an acceptance requirement behind it (§10), and the one most likely to find gaps left by groups 1–9. **10.1 and 10.2 are done** when the screens the golden session's ten beats show look finished at 1280×720 (D-97). Other screens get only the shared tokens, and neither task adds a feature. | 25 | Approved |
| D-152 | **The golden session end-to-end test** (10.4; §10, D-72, D-122). A third fixture, `golden-session`, seeds `session-1` under its own campaign ID. It then plays session 2's Beats 1–10 through the **HTTP routes**, in process (`app.inject`), streamed routes included, with scripted stub answers for the narrator, the checker and the planner. Each beat asserts its acceptance criteria and the golden session's numbers. **Dice** are the fixtures' loaded dice (D-122), not a seeded RNG, because they fail when the rules draw a die the script didn't supply. `buildApp` takes an optional `rng` for this, which every rolling route passes to its command; production omits it, so each command keeps `cryptoRandomSource`. `db:reset` seeds the fixture too, so the finished session can be opened in the browser. **A18 is not covered:** a stub answers instantly, so the 5-second target stays a live measurement. **`harness/golden-beats.ts` is retired.** It wrote Beats 3–9 by appending events directly, skipping the commands. Its two projection checks, a second read and a cold rebuild from the stored log, move into the new test, and `npm run harness` plays the new fixture in a throwaway schema. | 25 | Approved |
| D-153 | **Typography** (10.1; D-46, §8). The display face is an open-licensed (SIL OFL) font, **Oxanium**, bundled through `@fontsource` so it works offline on the home server; no CDN. It is used for headings, the scene title, outcome names and labels. Body text keeps the system stack, and numbers use tabular figures. | 25 | Approved |
| D-154 | **Docker Compose packaging** (10.5; D-55, §10). **One `app` image** built in stages (build the workspace, then a runtime image with production dependencies and the built `dist` folders), plus the existing Postgres service. The server serves the built web client with `@fastify/static` from the same address as the API, so the client's `/api` paths work unchanged, and unknown non-API paths fall back to `index.html` for client routing. The server migrates on start. **Configuration** comes from a `.env` file next to the compose file (`ANTHROPIC_API_KEY`, model overrides, `POSTGRES_PASSWORD`). **Production is not seeded:** `db:reset` and `db:seed` refuse under `NODE_ENV=production`, and the player creates a campaign in the app. Backups and TLS are out of scope. | 25 | Approved |
| D-155 | **Void-and-redo policy for a live play-through against real dice** (D-56's own definition of done). The golden session's script is written around specific outcomes; `cryptoRandomSource` won't reproduce them, and a live verification run shouldn't try to. When a roll lands on an outcome a beat is written around, and reaching that outcome is that beat's *unique* test — one no other beat and no deterministic fixture test already covers — **void and redo**, with the real reason recorded (the golden session's own Beat 7 already does this once, for a misdeclared stat, so the mechanic is native to the script). Otherwise, **accept the roll as it landed** and record the untaken branch as covered elsewhere, rather than voiding a correctly-played roll purely to manufacture a preferred outcome. Applied in the first live run: Beat 5's momentum-burn upsell was forced this way (nothing else exercises it); Beat 7's miss → Pay the Price → Endure Harm chain was not (it's exercised deterministically by `golden-session.test.ts`, D-152, and forcing it live would mean voiding a correctly-stated roll for its outcome alone). **This governs how the live play-through is re-run**, not how the golden session itself is scripted or how the automated test behaves. | 26 | Approved |

---

## 3. Authority Model — v1.2

| Decision | Owner | Status |
|---|---|---|
| Character actions, intent, thoughts, vows, spending resources | Player | Approved |
| Which move applies | Player picks directly, or asks the AI for a suggestion with a stated reason | Approved |
| Dice results, outcome tier, mechanical effects with no choice involved | Rules engine | Approved |
| Choices written into a move ("choose one…") | Player | Approved |
| What goes wrong on a miss | Player picks the Pay the Price method (default: roll the table); AI narrates | Approved |
| Complications on weak hits where the move offers no menu | Player writes it, or picks from AI-offered options | Approved |
| Severity of suffer moves (e.g., how much harm) | AI proposes from the fiction; player adjusts | Approved |
| NPC motives and actions, including betrayal | AI | Approved |
| New factions, locations, NPCs | AI, grounded in oracle rolls | Approved |
| Ticking threat and tension clocks | AI, always visible with a stated reason | Approved |
| Random results for world content | Rules engine rolls; AI interprets and may visibly reroll a result that doesn't fit | Approved |
| Correcting or retconning any AI-established fact | Player, at any time | Approved |

---

## 4. Oracle-Grounded Generation — v1.1 (Approved)

When the AI Guide creates story content (NPCs, locations, derelicts, factions, complications, answers to open questions about the world), it grounds that content in Starforged oracle rolls instead of inventing freely.

**Why.** It fits the spirit of the game, which uses oracles as a creative partner. It also answers the fairness concern: the world's surprises come from dice, not from an AI's taste. And oracle combinations push stories away from the familiar tropes language models drift toward.

**How.** The AI never produces random results itself. It asks the rules engine for rolls, which the server generates, and then interprets the results. Each roll is logged as an event recording the table, the value rolled, and the result, and it links to the narration it inspired. The dice supply the result; the AI supplies the interpretation. Rolls appear as chips under the narration. If a result doesn't fit the fiction, the AI rerolls it, and the discarded chip stays visible, struck through.

The AI sets the odds on its own yes/no questions about the world. Rerolls are capped per individual roll (campaign setting, default 2); once the cap is reached, the AI works with the final result (D-69). The AI rerolls without interrupting the beat — narration commits, the discarded chip stays visible with its reason, and the player may correct or reinstate it afterward (D-70).

**How many rolls per beat.** Each generated entity type has a declared **recipe** in the rules data: a named list of oracle tables and the slot each one fills. `npc` rolls role, goal, first look, disposition and name; `derelict` rolls condition and first looks (D-139: type depends on a location roll, and Milestone 1 frames an established derelict). The AI requests a recipe by name, the server rolls the whole set and returns the results with their slots, and the AI interprets them. Recipes make a beat's grounding reproducible, so the golden session can assert it (D-65).

---

## 5. Rules Scope — v1.1 (Approved)

**Rules source.** Astrolabe owns its internal rules schema. Datasworn's Starforged data (moves, assets, oracle tables) is imported into that schema through an adapter. Automation logic (effects, choices, chained moves) lives in Astrolabe's own layer, keyed to stable rule IDs. If Datasworn ever constrains the application, only the adapter changes, not the rules engine. Datasworn content is CC BY licensed, so the app includes an attribution screen.

**Automation levels**

| Level | What the app does |
|---|---|
| **Automated** | Rolls the dice, determines the outcome, applies effects that involve no choice, offers choices inline, and chains into follow-up moves |
| **Guided** | Rolls the dice and determines the outcome, highlights the matching outcome text, and gives quick controls so the player applies the effects |
| **Reference** | Shows the move text and rolls generically; the player updates state by hand |

**Assignment by milestone**

| Move category | Milestone 1 (golden session) | Milestone 2 | Later |
|---|---|---|---|
| Session, Adventure, Quest, Fate, Suffer | Automated | — | — |
| Threshold | Guided | — | Automated |
| Combat | Reference | Guided | — |
| Exploration, Recover | Reference | — | Automated (Sojourn stays Guided) |
| Connection, Legacy, Scene Challenge | Reference | — | Guided |

The Milestone 1 column is the *eventual* assignment for those categories. D-59 narrows what Milestone 1 actually builds: automation is hand-written only for the moves the golden session exercises, and every other move — including the Threshold moves — runs at Reference until a later milestone needs it. Datasworn supplies move text but no structured effects, so each Automated move costs a hand-authored effect specification; that cost is what the narrowing buys back.

**Assets.** Asset abilities are Guided. When a player picks a move, the abilities from their assets that apply to it are surfaced, and the player taps to apply them. Only assets used by the playtest characters get full automation.

**Relevant-moves panel.** Relevance is rules-based. It comes from explicit situation state that moves set and clear (for example, Enter the Fray puts the characters in a fight), plus categories that are always relevant. Relevance rules live in the rules data: each move declares the situation flags it requires, sets and clears. The full move list is always one click away. Milestone 1 needs none of that machinery exercised: every move in the Session, Adventure, Quest, Fate, Suffer, and Threshold categories has a broadly applicable trigger with no fictional-positioning requirement, so those six categories are always relevant and no situation flag is required until Milestone 2's Enter the Fray (D-66).

**Overrides and corrections.** Players can directly edit any meter, track, or clock. Every edit is logged with who made it, and it's shown differently from automated changes. Players can also void a roll and redo it. The voided roll stays in the log, visible and struck through *(display is Draft)*.

Voiding cascades over what the voided event caused — its effects, any clock it ticked, the narration it produced — and the player sees that set before confirming. It is refused when something outside that set has already been built on it, and it reaches back no further than the current session (D-83, D-84). The rule of thumb: **void is for "this shouldn't have happened," amendment for "this happened and was wrong."** Once the world has moved on, the correction path is a narration correction or a manual override, neither of which removes anything.

Milestone 1 delivers two of §3's three correction paths: narration correction (A15) and manual mechanical override (A16). Retconning an AI-established fact — amending an NPC's disposition, reinstating a discarded oracle result — is deferred (D-86). The authority in §3 is unchanged; only its delivery waits.

---

## 6. Campaign and Character Creation — v1.0 (Approved)

**Time budget.** About 30 minutes per character. Character creation happens before the session, on each player's own time, not as a shared activity at the table (confirmed by D-77 — see note below). Campaign setup (truths, sector, inciting incident) is a shared activity.

**Truths.** Each truth question offers three paths: pick from the book's options, roll for one, or write your own. All three are equally prominent.

**Starting sector.** A list of locations with the routes between them. Locations are created through oracle-grounded generation like any other world content. A visual starmap is a later phase.

**Character creation — concept-first.** The player writes a sentence or two describing the character they want. The AI proposes a complete build: paths and other assets, stat array, background vow, callsign, and backstory hooks, each with a one-line reason and grounded in oracles where it invents detail. The player reviews it as a whole, and every field remains directly editable, so a rules-fluent player can simply set their own stats and assets and skip the proposal. Nothing becomes canon until the player accepts it.

**Inciting incident.** The AI proposes several incidents, grounded in oracles and drawn from the characters' backgrounds and the campaign truths. The player picks one, edits it, or writes their own. The chosen incident becomes the first vow.

**Portraits.** AI-generated portraits for characters, ships, and NPCs, using a shared style so the campaign looks consistent. Deferred to a later phase.

**Deferred:** lines and veils, visual starmap, portraits, importing an existing campaign.

**Note on six-player setup.** At 30 minutes each, six characters created serially would consume an entire evening. Players therefore build characters independently between sessions, with the shared setup evening covering truths, the sector, the inciting incident, and crew bonds (D-77).

---

## 7. Golden Session — v1.0 (Approved)

**Purpose.** A scripted slice of ideal play, used as both the acceptance test and the fun test. If a feature doesn't appear in a golden session, it isn't MVP. The mechanics here are illustrative; exact move text and table results will come from the rules data.

**Setup.** Fresh campaign, session 2. Christopher plays three characters: **Vesna Kade** (pilot), **Rook Ilari** (ex-soldier), and **Juno Marr** (salvage tech), crewing the starship *Lantern Wake*. Active vow (formidable): recover the flight recorder of the colony ship *Meridian's Hope*. Narration latitude: Color. Momentum: Vesna +7, Rook +2, Juno +3.

### Beat 1 — Opening the session (0:00)
Christopher opens the campaign and clicks Begin Session. The AI gives a short "previously on…" built from last session's event log: the crew traced the recorder's beacon to a derelict relay station at the edge of the sector. The screen already shows the location, the vow and its progress, and every character's meters and momentum. Nothing has to be looked up.

*Tests:* recap from structured state, "you are here" at a glance, Begin a Session.

### Beat 2 — Framing the scene (0:02)
The AI frames the arrival. Before writing, it rolls derelict oracles through the engine, and the results appear as small oracle chips beneath the narration. The prose is rich because this is a scene opening. It ends on the stakes, without telling anyone what to do.

*Tests:* oracle-grounded generation, visible rolls, length scaled to the moment, no unrequested nudging.

### Beat 3 — A player decision gets depth (0:05)
Christopher selects Juno. The relevant-moves panel highlights the moves that fit the current situation. He picks Gather Information himself and types: "Juno jacks into the docking port and pulls the station logs." A player who typed the action without picking a move would get a suggestion instead, such as Gather Information +wits with a one-line reason. He rolls, and the result card reads **Weak hit, +1 momentum**, with the dice math a click away. The move doesn't specify the complication, so the app asks what complicates things. Christopher isn't sure and clicks **Give me options**. The AI offers three complications inspired by an Action + Theme roll, shown as chips. He picks one: the station was abandoned, yet one life-support circuit is still drawing power. The AI narrates at Color latitude: how Juno works, the fragmented evacuation logs, the humming circuit. Juno's thoughts are not narrated.

*Tests:* player-chosen move, relevant-moves panel, AI suggestion as fallback, outcome-first result with math on demand, weak-hit complication written by the player or chosen from AI options, narration within latitude.

### Beat 4 — The stall and the nudge (0:09)
Christopher isn't sure what's next and clicks **What now?** The AI offers three suggested actions. Each names the character best placed, the likely move, and why it matters now:
- Vesna traces the power draw with the *Lantern Wake*'s sensors (Gather Information).
- Rook secures the airlock before anyone goes deeper (Secure an Advantage).
- The crew pushes toward the station core (Undertake an Expedition).

Christopher combines two of them: Rook covers the airlock while Vesna runs the scan.

*Tests:* nudge only on request, suggestions anchored in current state, player free to remix suggestions.

### Beat 5 — Helping an ally, and a momentum decision (0:11)
Christopher picks Secure an Advantage for Rook and marks it as direct support for Vesna. The app applies Aid Your Ally and explains that the benefits go to her. Rook rolls a strong hit, so Vesna takes both benefits: +2 momentum and +1 on her next move. Vesna's scan is Gather Information +wits, with the +1 already applied. The result card reads **Weak hit**; clicking it shows an action score of 5 against challenge dice of 6 and 3. The app sees that Vesna's +7 momentum would beat both dice and offers: *"Burn momentum to upgrade to a strong hit? Momentum resets to +2."* Christopher accepts.

*Tests:* controlling multiple characters, benefits redirected to an aided ally, a rule the player didn't think to ask about surfaced when it matters, clear cost for a choice, outcome first with math on demand.

*(Amended under D-61: Secure an Advantage grants both benefits on a strong hit with no choice. The choice sits on the weak hit.)*

### Beat 6 — The world gets a new face (0:14)
On the strong hit, Vesna finds a heat signature: someone is alive aboard. The AI rolls character oracles for role, goal, first look, disposition, and name. One result contradicts what the evacuation logs established, so the AI rerolls it, and the discarded chip stays visible, struck through. An NPC card appears in the side panel, badged as AI-established. The AI narrates the first contact over comms, and the NPC's wariness comes straight from the disposition roll.

*Tests:* AI ownership of NPCs, oracle grounding, visible rerolls, automatic entity tracking, visible provenance.

### Beat 7 — A miss and the price (0:18)
Impatient, Christopher has Rook force the sealed bulkhead between the crew and the survivor. He picks Face Danger, chooses +edge by mistake, and rolls a strong hit. But Rook is forcing the bulkhead, not slipping past it, so Christopher voids the roll and redoes it with +iron. The voided roll stays in the log, struck through. This time it's a miss. The app presents Pay the Price with its options, with the table roll highlighted. He rolls, and the result says the character is harmed. The app chains into Endure Harm. The AI proposes −2 with a one-line reason: a ruptured conduit sprays sparks across Rook's arm, a serious burn. Christopher adjusts it to −1 because Rook's armor took the worst of it. Rook's health drops by 1, and the AI narrates the moment to match.

*Tests:* void and redo with a visible record, miss flow, player chooses the Pay the Price method, chained suffer move, AI-proposed severity adjusted by the player, narration that follows the adjusted outcome.

### Beat 8 — Pressure builds (0:20)
The AI creates a four-segment tension clock, "Station power failing," and fills one segment. The reason is shown with it: forcing the bulkhead tripped emergency load-shedding. The clock appears in the tracker panel, and hovering shows who ticked it and why.

*Tests:* AI ownership of clocks, every state change visible with its reason.

### Beat 9 — Corrections (0:22)
The narration calls Rook "shaken." Christopher disagrees: Rook is a veteran, annoyed rather than rattled. He flags the line, the AI rewrites that passage, and the event log records the correction. Nothing else changes. Christopher also remembers a ruling from last session that left Juno's momentum one too low. He edits it directly from +3 to +4, and the log records the manual override.

*Tests:* narration correction in a single action, narration respects player-owned character interiority, manual override of mechanical state, all corrections logged.

### Beat 10 — Ending the session (3:30)
Christopher clicks End Session. The AI writes a summary and lists open threads: the survivor's intent, the failing power, and where the recorder is. The app reminds him that Reach a Milestone is available if he judges one was earned. Everything is saved, and next session's recap will be built from it.

*Tests:* End a Session, open threads carried forward, vow progress under player control.

---

## 8. Play Screen — v1.0 (Approved)

**The stall test.** Three zones are always visible without scrolling, because together they answer "what now?":

- **Scene header** (centre, top) — where we are, what's at stake, what's unresolved.
- **Action composer** (bottom, full width) — relevant moves, the freeform "what do you do?" box, and the What now?, Ask Guide, and Oracle controls.
- **Pressure rail** (right) — clocks, vows, and progress tracks.

Only the narrative log scrolls. Everything else holds still.

**Layout**

| Zone | Contents |
|---|---|
| Top bar | Campaign, location, scene state, connection status |
| Left rail | Crew mini-cards, present NPCs, ship status |
| Centre | Scene header above a scrolling narrative log |
| Right rail | Clocks, vows, progress tracks |
| Bottom | Action composer |

**Scene header and narrative log together.** The log carries the prose, the rolls, and the oracle chips, and reads like a story. The header is a compact standing answer to "where am I and what's at stake," so nobody has to scroll back to find out. The AI maintains the header as play moves; it is not a second thing to write.

**Crew mini-cards** show callsign, health, and momentum. Spirit, supply, conditions, impacts, and assets open in a drawer on click. Six cards at this size fit the left rail without scrolling.

**Scenes, minimal ceremony.** Scenes are explicit in the data model, because clocks, recaps, and stakes all attach to them. In the UI they cost one click: when the situation changes, the AI proposes a new scene inline, and the player accepts, edits the title, or ignores it and keeps playing. There is no "start scene" wizard and no modal.

**Drawers, not navigation.** Clicking a crew card, an NPC, a clock, a vow, a move name, or an asset chip opens a drawer or popover over the play screen. Play never navigates away.

**Target devices.** Desktop and laptop. Tablet and mobile are out of scope.

**Dice.** Rolls are animated: the action die and the two challenge dice, distinguished as the book does. The animation is short enough not to slow a session and skippable *(Draft)*.

**Visual direction.** Starforged's direction, not its assets: dark surfaces, amber accents, strong display type for headings, and generous spacing. Progress tracks, clocks, condition meters, and momentum get purpose-built visual treatments. No art or page layout from the book is reproduced.

---

## 9. Architecture — v1.1 (Approved)

**Stack**

| Layer | Choice | Note |
|---|---|---|
| Front end | React + TypeScript, Vite | |
| Server | Node + TypeScript | |
| Database | PostgreSQL | |
| Packaging | Docker Compose for production; native for development | |
| Auth | None in Milestone 1; Authentik via OIDC with multiplayer | |
| AI | Provider abstraction over Claude and OpenAI, streaming | |

**Why TypeScript on the server.** The rules engine is the heart of the app, and both sides need it: the client to show relevant moves, valid choices, and modifiers, and the server to be authoritative over dice and state changes. In one language it is a single shared package with one set of types. Split across two languages, every rule type gets defined twice and drifts. The alternative worth weighing is ASP.NET Core, which is closer to daily work, at the cost of duplicating the rules model or pushing all rules logic server-side and making the UI chattier.

**Shape.** A monorepo with four packages:

- `rules` — the rules schema, the Datasworn adapter, move automation logic, and dice. Pure functions, no I/O, heavily tested. Shared by client and server.
- `server` — HTTP API, the event log, state projection, the AI provider abstraction, and prompt assembly.
- `web` — the React client.
- `shared` — event and DTO types.

**Server-authoritative state.** All dice and all state changes go through the server. The client renders state and sends intent. This is unnecessary for solo play and essential for multiplayer, and retrofitting it later would mean rewriting every interaction. It costs little now.

**Event log.** Every state change is an appended event: rolls, move resolutions, oracle rolls, clock ticks, canon entries, narration, corrections, and manual overrides. Current state is a projection over the log. The log gives recaps, AI grounding, void-and-redo, correction history, and later multiplayer sync, all from one mechanism.

Three properties make that work, and each constrains what may go in an event:

- **Projection is a pure fold.** No dice, no clock, no I/O, and no reads of the rules data. Anything non-deterministic — a die, an oracle row, an AI passage, a generated ID, a timestamp — is resolved once at write time and stored.
- **Events store resolved effects, not instructions to recompute them.** Which effects a weak hit produces comes from the automation layer, which changes when Datasworn is regenerated or a spec is edited. Replaying an old campaign must not produce different numbers, so the resolved deltas are recorded. Bounds that should track the current rules — momentum's maximum, for instance — stay derived. *Store facts, derive bounds.*
- **Events record causality.** Each carries the command that wrote it and, where one exists, the event that caused it. That is what makes cascading void computable and what groups a narrative log into beats rather than rows.

Detailed design: [`design-event-log.md`](design-event-log.md).

**Real-time.** Deferred with multiplayer, but the design assumes it: events already carry an actor, and the client already applies server-authored events. Adding a WebSocket later means broadcasting the events that are already being written, not restructuring state.

**AI provider abstraction.** One interface with streaming, structured output, and token accounting. Claude and OpenAI implementations behind it. Prompt assembly and context selection live above the interface, so switching providers does not change how context is built.

**AI outage.** If the provider is unavailable, the session stops. State is unaffected, because narration is an event like any other and everything else is already committed. A resumed session picks up from the last committed event.

---

## 10. Non-Functional Requirements — v1.0 (Approved)

| Requirement | Target |
|---|---|
| Narration begins streaming | Within 5 seconds |
| Dice, state changes, and panel updates | Immediate; never gated on the AI |
| Token accounting | Visible per session |
| State durability | No committed event is ever lost, including through an AI failure |
| Rules engine and dice | Unit tested, including outcome tiers, matches, momentum burn, and chained moves |
| Golden session | Runs end to end as an automated test |
| AI output | Validated against its schema; rejected and retried on failure. Quality is not unit tested |
| Deployment | One `docker compose up` on Linux |
| Accessibility | Keyboard navigable; meaning never carried by color alone |

---

## 11. Milestone Plan — v1.0 (Approved)

**Milestone 1 — the golden session.** Done when [`golden-session.md`](golden-session.md) runs start to finish against a real campaign, real rules data, and real AI narration. Scope and tasks: [`milestone-1.md`](milestone-1.md).

**Milestone 2 — combat.** Guided combat moves, fight state (position and progress), and a combat beat added to the golden session.

**Milestone 3 — the table.** Real-time multiplayer, Authentik authentication, player presence, spotlight, and Aid Your Ally across separate clients.

**Later, unordered.** Automated exploration and recovery moves, connections, legacy tracks, scene challenges, the visual starmap, AI-generated portraits, lines and veils.

---

## 12. Fun Budget — v1.0 (Approved)

Experience targets, binding alongside §10.

| Target | Proposed threshold |
|---|---|
| Declared intent → visible roll result | 3 interactions or fewer |
| Narration starts appearing | Within 5 seconds (streamed) |
| Routine-beat narration length | Short (roughly 60–120 words), longer for scene openings and dramatic outcomes |
| Answering "what can I do?" | Never requires leaving the play screen |
| State change visibility | Shown in the same beat that caused it, with a reason |
| Correcting AI narration | One action |
| Resuming a campaign | Recap plus "you are here" readable in under a minute |
